import { FormatViewsPipe } from './format-views.pipe';

describe('FormatViewsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatViewsPipe();
    expect(pipe).toBeTruthy();
  });
});
